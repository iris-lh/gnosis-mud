'use strict';

/**
 * Maining list of careers in the game
 * @see PlayerCareer
 */
class CareerManager extends Map {
  /*
   * Careers right now have no general "manager" like needs besides having a
   * list of careers but this career is used regardless in case that need arises
   * in the future.
   */
}

module.exports = CareerManager;

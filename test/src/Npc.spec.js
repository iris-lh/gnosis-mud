const mocha = require('mocha');
const { expect } = require('chai');

xdescribe("Npc", () => {

  beforeEach(()=>{

  })

  describe("Method", () => {
    it("Should pass", () => {
      // TODO
    });
  });

});

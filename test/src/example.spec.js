const mocha = require('mocha');
const { expect } = require('chai');

describe("Testing", () => {

  beforeEach(()=>{
    
  })

  describe("Method", () => {
    it("Should pass", () => {
      expect(true).to.be.true;
    });
  });

});
